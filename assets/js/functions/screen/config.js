const col = 30;
const row = 30;
const px = 15;
const canvasWidth = col * px;
const canvasHeight = row * px;


const _configScreen = {
    col,
    row,
    px,
    canvasWidth,
    canvasHeight
};

export default _configScreen;